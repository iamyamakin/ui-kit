import React from 'react';
import { storiesOf } from '@storybook/react';

const icon = () => {
    return (
        <span>TODO: add Icon component</span>
    );
};

storiesOf('Components', module).add('Icon', icon);
